@page
@model WAWillClinicFrontEnd.Pages.DetailsModel
@{
    Layout = "_Layout";
}
<section>
    <h2>Edit user</h2>
</section>

<section>
    <form method="post">
        <input type="text" name="Name" value="@Model.Name" required title="Please input a name" />
        <br />
        <input type="email" name="Email" value="@Model.Email" required title="Please input an email address" />
        <br />
        <input type="tel" name="Phone" value="@Model.Phone" required  pattern="[0-9]{10}" title="Please input a phone number" />
        <br />
        <input type="submit" value="Update User" />
    </form>
    <form method="post" asp-page-handler="delete">
        <input type="hidden" name="Email" value="@Model.Email" />
        <input type="submit" value="Delete User" />
    </form>
</section>